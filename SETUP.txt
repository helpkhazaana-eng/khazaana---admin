KHAZAANA CUSTOMER NOTIFICATION FIX

Upload/replace these 3 files in the SITE GitHub repository:
1. track-order.html
2. firebase-messaging-sw.js
3. server.js

Then commit the changes.

Android Chrome:
- Open the Track Order page.
- Tap "Enable Order Updates".
- Tap Allow.

iPhone Safari:
- Open Khazaana from the Home Screen app.
- Tap "Enable Order Updates".
- Tap Allow.
- iPhone Web Push requires a supported iOS version and Home Screen web app.

Do not upload the service worker or server.js to a different folder.
firebase-messaging-sw.js must be in the same directory as track-order.html.
